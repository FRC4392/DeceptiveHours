[build]
  command = "npx convex deploy --cmd 'bun run build'"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.deploy-preview]
  command = "bun run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
